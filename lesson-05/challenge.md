# Lesson 5 Challenge

## 1. What is a Python keyword?

These are special words that already have meaning in python

## 2. Give three examples of Python keywords.

class, if, else

## 3. Can `if` be used as a variable name? Why?

no, it is already a keyword in python

## 4. What is syntax?

rule used in writing python codes correctly

## 5. What happens when Python finds a SyntaxError?

it stops and prints an error

## 6. What is indentation?

spacing correctly at the begining of a line

## 7. What happens when indentation is incorrect?

an indentation error

## 8. What does `#` do in Python?

starts a comment that gets ignored when the code runs

## 9. Are comments executed by Python?

no

## 10. What is a built-in function?


words we can use in python without creating it ourselves

## 11. Give three examples of Python built-in functions.

print() 
type() 
len()


## 12. What is the difference between a keyword and a built-in function?

A keyword is a special word that is part of Python's language syntax, while a built-in function is a function provided by Python that we can use.


## 13. What does `len()` do?

len() returns the number of characters in a string or the number of items in something such as a list.

## 14. What does `type()` do?

shows the type of variable

## 15. What is the difference between SyntaxError and IndentationError?

syntaxError has to do with breaking python rules while indentationError occurs when there is a wrong line spacing